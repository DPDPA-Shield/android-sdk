# Required for any Android module that depends on AndroidX (this one depends
# on androidx.core, androidx.security.crypto, androidx.datastore, and every
# androidx.compose.* artifact) - AGP refuses to resolve them without this,
# confirmed against a real build ("AndroidX dependencies detected but
# android.useAndroidX property is not enabled").
android.useAndroidX=true